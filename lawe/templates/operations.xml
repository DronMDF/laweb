<?xml version="1.0" encoding="UTF-8"?>
{% load static %}
<?xml-stylesheet type="text/xsl" href="{% static 'operations.xsl' %}"?>
<operations>
	<cqrf>
		{% csrf_token %}
	</cqrf>
{% for acc in accounts %}
	<account>
		<id>{{acc.id}}</id>
		<name>{{acc.name}}</name>
	</account>
{% endfor %}
{% for op in operations %}
	<operation>
		<debit>
			<id>{{op.debit.id}}</id>
			<name>{{op.debit.name}}</name>
		</debit>
		<credit>
			<id>{{op.credit.id}}</id>
			<name>{{op.credit.name}}</name>
		</credit>
		<amount>{{op.amount}}</amount>
		<description>{{op.description}}</description>
	</operation>
{% endfor %}
</operations>
