<?xml version="1.0" encoding="UTF-8"?>
{% load static %}
<?xml-stylesheet type="text/xsl" href="{% static 'operations.xsl' %}"?>
<operations>
	<user>
		<name>{{request.user.username}}</name>
	</user>
	<cqrf>
		{% csrf_token %}
	</cqrf>
{% for acc in accounts %}
	<!--
	@todo #55:30min Необходимо добавить сюда признак доступности счета
	 Чтобы можно было отфильтровать доступные счета для формы
	 И при этом использовать данные отсюда в таблице ниже
	 (а не хранить имена и там)
	-->
	<account>
		<id>{{acc.id}}</id>
		<name>{{acc.name}}</name>
	</account>
{% endfor %}
{% for op in operations %}
	<operation>
		<date>{{op.date}}</date>
		<debit>
			<id>{{op.debit.id}}</id>
			<name>{{op.debit.name}}</name>
		</debit>
		<credit>
			<id>{{op.credit.id}}</id>
			<name>{{op.credit.name}}</name>
		</credit>
		<amount>{{op.amount}}</amount>
		<unit>{{op.unit}}</unit>
		<description>{{op.description}}</description>
	</operation>
{% endfor %}
</operations>
